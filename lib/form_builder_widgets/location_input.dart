import 'package:flutter/material.dart';

class LocationImage extends StatelessWidget {
  const LocationImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
