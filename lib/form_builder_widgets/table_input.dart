import 'package:flutter/material.dart';

class TableInput extends StatelessWidget {
  const TableInput({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
